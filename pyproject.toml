[project]
name = "database-toolkit"
version = "0.1.0"
description = "A database utilities package for Python developers"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"